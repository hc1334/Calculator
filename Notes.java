package project4;

public class Notes {

	/*
	 * args (has to be length 2, or else print error)
	 * args(0) is name of input file
	 * args(1) nam eof output file
	 * catch dividing by zero 
	 * 
	 * generic stack
	 * 	operator and numbers
	 * 
	 * use scanner object to parse string (what??)
	 * Scanner tokenizer new Scanner(expression);
	 * 
	 * 
	 */
}
